# RQ378 evidence

- Date: 2026-09-25
- Task ID: RQ378
- Queue: `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- Delivery target: `main`
- Working branch / PR: `cursor/supplier-sales-attribution-b591` / [PR #68](https://github.com/ivanjovicic/Trendplus/pull/68)
- Evidence state: synchronized

## Interpreted outcome and owner

Supplier Sales must use one covered-revenue-weighted margin benchmark for
`totals.prosecnaMarza` and recommendation evaluation. Cost quality must
distinguish historical sale-line cost, snapshot fallback, product fallback and
truly unavailable cost without turning unknown coverage into a trusted value.
The backend endpoint is the source of truth; the Supplier page only renders
the explicit coverage fields.

## Changed files

- `Api/Endpoints/AllEndpoints.cs`
- `Api.Tests/AnalyticsSupplierSalesIntegrationTests.cs`
- `Klijent/clientapp/src/pages/SupplierSalesStatsPage.tsx`
- `Klijent/clientapp/src/services/supplierSalesStatsApi.ts`
- `MASTER_ROADMAP.md`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`

## Runtime behavior

- Replaced the known-supplier arithmetic mean with
  `MarginContribution / RevenueWithCost`, preserving `null` when the covered
  denominator is unavailable.
- The same weighted value is passed to the recommendation engine and exposed
  as `totals.prosecnaMarza`, with weighted revenue/contribution and benchmark
  basis metadata.
- Added explicit cost coverage partitions and a cost-source precedence field.
- Corrected Supplier page toolbar/KPI wording so historical coverage and
  truly unavailable cost are not conflated with snapshot/product fallback.

## Checks run

- `npm run test:run -- src/pages/__tests__/SupplierSalesStatsPage.decisionSuppliers.spec.tsx src/pages/__tests__/SupplierSalesStatsPage.premium.spec.tsx` — pass, 33/33.
- `npm run check:analytics-guardrails` — pass; encoding, guardrail baseline and TypeScript checks passed.
- `npm run build` — pass.
- `node scripts/check-agent-instructions.mjs --self-test` and validator — pass.
- `node scripts/check-prompt-queues.mjs --self-test` and validator — pass, 547 tasks.
- `node scripts/check-planning-architecture.mjs --self-test` and validator — pass, 78 planning tasks.
- `git diff --check` — pass.

## Checks not run

- `dotnet test Api.Tests/Api.Tests.csproj --no-restore --filter FullyQualifiedName~AnalyticsSupplierSalesIntegrationTests` — unavailable: the VM has no `dotnet` executable.
- Backend build and live PostgreSQL/provider integration — not run for the same unavailable SDK/database environment.
- Full frontend/backend suites, browser smoke and remote CI — not required by the focused selector and not inspected.

## Delivery and verification

- Claim and implementation commits were pushed on the feature branch before validation.
- Main delivery required a non-conflicting merge from concurrent `origin/main` updates.
- Main commit SHA: `5ece51a0a442b0433a69822a1dc642431c2be1e9`
- Main verification: passed — `git rev-parse HEAD` equals `git rev-parse origin/main`.
- Local RQ378 lock was removed before delivery.

## Missed and residual risk

- Backend runtime execution against the integration database remains unproven
  because the SDK and local provider were unavailable.
- Existing downstream consumers must tolerate the added optional metadata;
  the response remains backward-compatible.
- RQ379 runtime schema validation and RQ380 comparable pre/post totals remain
  separate queued owners.

## Documentation impact

- Promoted and claimed RQ378, then synchronized its completion to `DONE`.
- Updated the canonical roadmap current-READY truth and durable evidence link.
