# RQ444 evidence — Supplier overview period truth

Task ID: RQ444
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-25
Agent/tool: grok
Delivery target: main (direct-main)
Working branch: none - implemented and tested in the detached worktree `C:\Users\Ivan\source\repos\Trendplus2-grok`, committed directly on local `main` after the RQ443 commit
Main commit SHA: `53f66cfa2c2d5ca226da4242ccb68d0add69c889`
Main verification: passed - `git merge-base --is-ancestor 53f66cfa origin/main` succeeded on 2026-09-25 (DONE sync)
Evidence state: synchronized

## Root causes

1. `useSupplierCanonicalState.setPreset`/`setDate` never removed the legacy `sezonaId`; the embedded overview reads it from the URL (`SupplierSalesStatsPage.tsx:783`, `:856`) and the endpoint replaces the date range with the season (`AllEndpoints.cs:1161-1175`), so a legacy link pinned the KPIs to the season while the controls showed other dates.
2. The embedded trust payload set `effectivePeriodLabel` to the whole-history sales data window, and `SupplierConsolidatedPage` shows that label as „Period i filteri“.
3. The payload forwarded `toDate` as `…T23:59:59Z`; `formatDate` renders local time, so Europe/Belgrade saw the next day.

## Change

- `useSupplierCanonicalState.ts`: `setPreset` and `setDate` delete `sezonaId`; other setters keep it; `resetFilters` unchanged.
- `SupplierSalesStatsPage.tsx`: `buildSupplierEmbeddedPeriod` returns calendar-date `periodFrom`/`periodTo` (UTC date of the response range, fallback to the requested dates) and `effectivePeriodLabel` = analyzed period with a „dostupni podaci: …“ data-window suffix. The standalone header still shows the data window (existing spec kept).
- Tests: new `pages/__tests__/useSupplierCanonicalState.spec.tsx` (3 cases); two cases in `SupplierSalesStatsPage.premium.spec.tsx`.
- `scripts/known-guardrail-baseline.json`: line shifts only (`fake_zero_fallback` 1832 → 1882, `refetch_setData_null` 858 → 899).

## Validation (worktree)

- New tests against the RQ443 state: 4 failed (both hook period cases, both payload cases); the third hook case (supplier change keeps `sezonaId`) passes before and after. After the change: all pass.
- Focused specs `SupplierSalesStatsPage.premium`, `SupplierSalesStatsPage.decisionSuppliers`, `SupplierConsolidatedPage`, `SupplierRedirects`, `useSupplierCanonicalState`, `AppAnalyticsRoutes`: 74/75; the single failure is the pre-existing `RQ440` case in `SupplierConsolidatedPage.spec.tsx`.
- `npx tsc -b`: pass. `npm run check:analytics-guardrails`: pass with the updated baseline.
- Queue validators (all six) and `git diff --check`: pass (commit tool, clean export).
- Full frontend suite: see `.ai/runs/2026-09-25-supplier-sales-overview-audit-evidence.md`.

## Not run / residual risk

- Live data proof: not possible (API hosts 503).
- `RQ442` will change the request end to an exclusive next-day bound; it must keep the calendar-date display (noted in `RQ444` dependencies).
- Data Quality return links still carry `sezonaId` when the user arrived with one; it is dropped as soon as the period is changed.
- DONE sync 2026-09-25: `origin/main` contains `53f66cfa`; queue status moved `PARTIAL -> DONE`.
