# RQ155 Evidence

- Date: 2026-09-06
- Task: RQ155 - Keep unknown Dashboard trends visible and non-ranked
- Queue: RQ155
- Owner: Codex, resumed from the closed partial local claim
- Delivery: direct `main`

## Outcome

Dashboard gain/loss lists now include only finite measured positive and negative trends. Null, missing, `NaN` and `Infinity` are excluded from ranking; genuine zero remains a measured neutral value and is displayed as `Bez promene 0%`. Unknown and non-finite values remain visible in the existing top-product table as `Nema trenda` with an unavailable percentage. CSV export leaves the trend cell empty for unknown/non-finite values rather than emitting `NaN` or `Infinity`.

No backend trend formula, ordering contract, recommendation, score or confidence semantics were changed.

## Validation

- `npm run test:run -- src/pages/__tests__/AnalyticsDashboard.tableSystem.spec.tsx` - passed, 4/4.
- Combined Daily Sales and Dashboard focused suite - passed, 16/16.
- `npm run check:analytics-guardrails` - passed.
- `npm run typecheck` - passed.
- `npm run build` - passed. Vite emitted only the existing large-chunk advisory.
- `git diff --check` - passed for the delivered scope.

## Required counterexamples

- Null/missing trend: absent from gain/loss ranking and visible as unavailable.
- `NaN`/`Infinity`: absent from ranking, table state remains unavailable and export is blank.
- Genuine zero: absent from gain/loss ranking but visible as measured neutral zero.
- Existing table/export parity fixture: remained green.

## Boundaries and residual risk

Backend build/tests, live database/schema/migration/404/refresh proof, browser console/theme/chart smoke and full cross-route report/export parity were not claimed. PDC `RQ157` is a separate active local claim and was not touched.

The implementation is present on current `main` through `3ce9d86e` and `568aaf7d`; this evidence was run after those commits. Queue status and lock cleanup are delivered separately.
