Task ID: RQ295
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-17
Agent/tool: Codex
Delivery target: main
Working branch / PR: main (direct delivery)
Main commit SHA: b851afa2
Main verification: passed - `origin/main` contains implementation `b851afa2`.
Evidence state: synchronized

## What was done
- Promoted the first safe P1 WAITING prompt because the queue had no READY prompt, then claimed RQ295.
- Replaced the direct Pre-Nivelacija fetch failure interpolation with a bounded safe error boundary: empty/network/timeout failures receive non-empty Serbian guidance, structured JSON can retain safe detail plus correlation metadata, and HTML/raw technical bodies are suppressed.
- Passed safe error fields to the page error surface while preserving retry and error-versus-empty separation.
- Added focused API/page regression coverage for empty body, safe JSON/correlation, HTML, network/timeout, direct/invalid scope and mounted scope reload behavior.

## Files changed
- Klijent/clientapp/src/services/preNivelacijaApi.ts
- Klijent/clientapp/src/services/__tests__/preNivelacijaApi.scope.spec.ts
- Klijent/clientapp/src/pages/PreNivelacijaPriorityPage.tsx
- Klijent/clientapp/src/pages/__tests__/PreNivelacijaPriorityPage.spec.tsx
- docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
- MASTER_ROADMAP.md

## Validation run
- `npm run test -- --run src/pages/__tests__/PreNivelacijaPriorityPage.spec.tsx src/services/__tests__/preNivelacijaApi.scope.spec.ts` -> pass (2 files, 17 tests)
- `npm run typecheck` -> pass
- `npm run check:analytics-guardrails` -> pass (encoding and guardrails included)
- `npm run build` -> pass (existing Vite chunk-size advisory only)
- `git diff --check` -> pass

## Validation not run
- Full frontend/backend suites, live API/database/browser smoke and remote CI -> not run; no live provider was required for this client error-contract change.

## Documentation impact
- Queue and master roadmap promotion/claim/completion pointers were updated; queue is back to no READY prompt after delivery.

## What was missed
- No live proxy/backend payload or browser rendering proof; mocked response classes cover the specified failure matrix.

## Risks
- The direct endpoint remains a page-specific fetch rather than the shared retrying analytics HTTP layer; this run keeps its existing request shape and hardens only its visible failure boundary.

## Next
- After delivery, return the RQ pointer to no READY prompt; RQ296 remains the next WAITING candidate for explicit promotion.
