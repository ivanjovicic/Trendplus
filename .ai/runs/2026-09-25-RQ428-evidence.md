# RQ428 evidence — Daily Sales receipt identity reconciliation

Date: 2026-09-25
Task: RQ428
Owner: Analytics Backend / Daily Sales
Queue: formal queue prompt; claimed after collision check because RQ427 is actively owned by `RQ427-grok.lock.md`.

## Outcome

Daily Sales receipt-mismatch diagnostics now join only on the authoritative available cross-source identity:

`normalized BrojRacuna + UTC calendar day + IDObjekat`

`DnevnikPromena` has no foreign key to `ProdajaZaglavlje`, so coincident database IDs are never used as a match. Receipt-line and journal totals are summed with their original sign. Missing journal receipt identity returns `status: unavailable` with reason `dnevnik_receipt_identity_missing`; mismatch count and amount remain `null`, never fake zero.

## Files

Changed:

- `Api/Services/DailySalesStatsService.cs`
- `Api/Models/DailySalesStatsDto.cs`
- `Api.Tests/DailySalesReceiptReconciliationTests.cs`
- `Api.Tests/DailySalesStatsServiceTests.cs` (UTF-8 assertion aligned with the existing localized warning)
- `Klijent/clientapp/src/services/dailySalesStatsApi.ts`
- `Klijent/clientapp/src/pages/DailySalesStatsPage.tsx`

Existing unrelated work was preserved, including active RQ427 Inventory files and other queue/audit edits.

## Validation

- `dotnet test Api.Tests/Api.Tests.csproj --filter FullyQualifiedName~DailySales` — **33/33 passed**.
- `dotnet test Api.Tests/Api.Tests.csproj --filter FullyQualifiedName~DailySalesReceiptReconciliationTests` — **6/6 passed**.
- `npm run check:analytics-guardrails` — encoding and guardrail self-test passed; typecheck completed without errors.
- Daily Sales page specs: `21/22` passed; one pre-existing unrelated `supplier-concentration-warning` expectation remains failing in `DailySalesStatsPage.premium.spec.tsx` and is outside RQ428's receipt reconciliation scope.
- `git diff --check` — passed.

## Not completed / residual risk

- Live PostgreSQL proof was not part of RQ428 and was not run here.
- The bounded in-memory/EF regression suite proves the join contract and fail-closed behavior; production data should still be exercised by the existing live proof gate.
- The unrelated supplier-concentration frontend test failure is recorded honestly and was not changed.

## Delivery

Target: `main`.
The task lock is removed before delivery. Exact main SHA and remote verification are recorded in the final completion note after commit/push.
