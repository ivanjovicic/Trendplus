# RQ445 run log — Shoe Type trust header shows the selected calendar period

Task ID: RQ445
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-25
Agent/tool: Grok (executor, Ivan's local worktree)
Delivery target: local `main` only (owner instruction: local commits, never push)
Main commit SHA: pending - local `main` commit `fix(analytics): show the Shoe Type trust period as calendar dates (RQ445)`
Main verification: pending - not pushed
Evidence state: pending

## Problem

`ShoeTypeSalesStatsPage.tsx:973-974` passed the raw response `fromDate`/`toDate` to the trust header. The response echoes the request end `…T23:59:59Z` (season `…T23:59:59.9999999Z`), and `formatDate` (`utils/analyticsFormatters.ts:61-70`) renders local time, so in Europe/Belgrade the header period ended one day after the selected end date.

## Change

- `periodFrom`/`periodTo` now receive `toDateOnly(data.fromDate/toDate)` (UTC calendar date of the effective range, existing helper at `:185-189`), falling back to the requested filter dates. No request, KPI, export or backend change; line count unchanged (guardrail baseline untouched).
- New `src/pages/__tests__/ShoeTypeSalesStatsPage.periodTruth.spec.tsx` (2 cases).
- `src/pages/__tests__/ShoeTypeSalesStatsPage.premium.spec.tsx:327-328` asserted the raw timestamps (it encoded the defect); it now expects `2026-06-01`/`2026-06-30`.

## Validation

- New spec on the old page: 2/2 failed (header received `2026-06-01T00:00:00Z|2026-06-30T23:59:59Z`); after the fix 2/2 passed.
- Focused Shoe Type set (base spec, premium, three `utils/__tests__/shoeType*` specs, `AppAnalyticsRoutes.spec.tsx`, both new specs): 104/104 passed with `RQ446` applied; with only `RQ445` applied the only failures were the two `RQ446` cases. Baseline before any change: 80/80 over the five pre-existing Shoe Type specs.
- `npx tsc -b`: pass. `npm run check:analytics-guardrails`: pass (no mojibake, self-test, 50 known / 0 removed, typecheck).
- Queue validators (six) and `git diff --check`: pass (commit tool, clean `git archive` export).
- Not run: live API/DB proof (Vercel serves SPA HTML for `/api`, no local API/DB), CI, `dotnet test` (no backend change).

## Residual risk

- The request end stays `T23:59:59Z` until `RQ442`; the header still shows the analyzed calendar period after that change.
