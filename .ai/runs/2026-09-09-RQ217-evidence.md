Task ID: RQ217
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-09
Agent/tool: Codex
Delivery target: main
Working branch / PR: main / direct-main
Main commit SHA: pending
Main verification: pending
Evidence state: pending

## What was done

- Explicitly promoted the next safe queue prompt, RQ217, from WAITING to READY and claimed it as IN_PROGRESS.
- Changed `OutboxProcessorWorker` to claim pending messages in a transaction using `FOR UPDATE SKIP LOCKED`, with commit/rollback handling around the batch.
- Added a unique `(SaleId, ProductId)` analytics index and a migration that fails closed when historical duplicate pairs exist; no data was silently deleted.
- Added focused source/model contract coverage and a PostgreSQL contention test for two transactions attempting to claim the same message.

## Files changed

- `Workers/OutboxProcessorWorker.cs`
- `Infrastructure/DbContexts/AnalyticsDbContext.cs`
- `Infrastructure/Migrations/AnalyticsDb/AnalyticsDbContextModelSnapshot.cs`
- `Infrastructure/Migrations/AnalyticsDb/20260909190000_AddSalesLineFactIdempotency.cs`
- `Api.Tests/OutboxProcessorWorkerConcurrencyTests.cs`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `MASTER_ROADMAP.md`

## Validation run

- `dotnet build Workers/Workers.csproj --no-restore --verbosity:minimal` -> pass, 0 warnings and 0 errors.
- `dotnet build Api.Tests/Api.Tests.csproj --no-restore --verbosity:minimal` -> pass, 0 errors; existing analyzer warnings remain.
- `dotnet test Api.Tests/Api.Tests.csproj --no-build --filter "FullyQualifiedName~OutboxProcessorWorkerConcurrencyTests"` -> pass, 2/2; the provider-backed test returned early because the local PostgreSQL fixture was unavailable.
- `dotnet ef migrations list --project .\Infrastructure\Infrastructure.csproj --startup-project .\Api\Api.csproj --context AnalyticsDbContext --no-build` -> pass; new migration was discovered. Database application status was unavailable because configured remote credentials rejected authentication.
- `node scripts/check-agent-instructions.mjs --self-test` -> pass.
- `node scripts/check-agent-instructions.mjs` -> pass.
- `node scripts/check-prompt-queues.mjs --self-test` -> pass.
- `node scripts/check-prompt-queues.mjs` -> pass.
- `node scripts/check-planning-architecture.mjs --self-test` -> pass.
- `node scripts/check-planning-architecture.mjs` -> pass.
- `git diff --check` -> pass; Git reported only expected LF/CRLF normalization warnings.

## Validation not run

- Live PostgreSQL/Testcontainers concurrency proof -> not run; Docker provider was unavailable locally.
- Full backend test suite -> not run; the focused proof was sufficient for this bounded change.
- Production worker/broker smoke and remote CI -> not run; no production mutation or broker delivery was required.

## Documentation impact

- Promoted and closed RQ217 in `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`.
- Updated RQ routing state and completion pointer in `MASTER_ROADMAP.md`.
- Added the required scope/read/test/dependency sections to the legacy prompt as a recorded same-owner scope repair.

## What was missed

- No live provider-backed concurrent worker execution was available locally.
- No authenticated remote migration application was attempted.

## Risks

- The new migration intentionally fails closed if existing duplicate `(SaleId, ProductId)` rows are present; an approved cleanup must precede applying it to such a database.
- Exactly-once broker publication is outside RQ217 and remains a separate delivery concern.
- The test fixture follows the repository convention of returning without assertions when PostgreSQL/Testcontainers is unavailable; this is recorded explicitly above.

## Next

- RQ218 remains WAITING and should be explicitly promoted only when ready.
- After delivery, synchronize this log and the queue completion note with the exact implementation SHA and verify local `HEAD == origin/main`.
