Task ID: RQ268
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-15
Agent/tool: Codex
Delivery target: main
Working branch / PR: main (direct delivery)
Main commit SHA: pending
Main verification: pending
Evidence state: pending

## Interpreted outcome and owner

- Outcome: keep legacy Supplier Operations links deep-linkable while making their canonical navigation context explicit.
- Owner: Analytics Frontend / Navigation Shell.
- Source of truth: `/analytics/supplier` is the canonical Supplier page; the legacy Operations routes remain compatibility entry points and resolve to the matching canonical tab.
- Assumption: canonical `Pregled dobavljača` remains the one active navigation item; a safe query marker plus visible canonical-page notice is the smallest reversible way to explain the redirect without creating two active owners.

## What was done

- Promoted RQ268 from WAITING to READY because the canonical RQ queue had no current READY prompt, then claimed it as the single active prompt.
- Preserved every existing query parameter in `SupplierRedirects.tsx`, set the canonical `tab`, and added a safe `legacySource` marker for the two Operations compatibility links.
- Added a canonical Supplier notice that explains which Operations link was redirected and which canonical tab is active; unknown marker values fail closed and are not rendered.
- Kept `Navigate replace`, so browser Back returns to the page before the compatibility entry instead of re-entering the redirect loop.
- Added focused redirect, canonical-tab and Sidebar active-link tests.

## Files changed

- `Klijent/clientapp/src/pages/SupplierRedirects.tsx`
- `Klijent/clientapp/src/pages/SupplierConsolidatedPage.tsx`
- `Klijent/clientapp/src/pages/SupplierConsolidatedPage.css`
- `Klijent/clientapp/src/layout/components/__tests__/Sidebar.spec.tsx`
- `Klijent/clientapp/src/pages/__tests__/SupplierConsolidatedPage.spec.tsx`
- `Klijent/clientapp/src/pages/__tests__/SupplierRedirects.spec.tsx`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `.ai/runs/2026-09-15-RQ268-evidence.md`

## Validation executed

- Focused navigation/route matrix: 6 files / 24 tests passed, including App route smoke, both legacy redirects, canonical tab preservation and exactly one active canonical Sidebar link.
- `npm run check:analytics-guardrails`: passed; encoding, analytics guardrails and typecheck passed.
- `npm run build`: passed; Vite production build completed with the existing large-chunk advisory only.
- `node scripts/check-prompt-queues.mjs`: passed after promotion/claim; final completion synchronization remains to be validated.
- `git diff --check`: passed.

## Validation not run and residual scope

- Live backend/browser/deployed manual smoke, browser history instrumentation and axe accessibility scan were not run.
- Full frontend suite was not run.
- `SupplierDecisionHubRedirect` remains unchanged because this prompt owns the two legacy Operations entries; it requires a separately bounded navigation decision if included later.
- No compatibility route was removed and no backend/API contract was changed.
- Existing query values are preserved intentionally, while only known safe `legacySource` markers produce user-facing copy.
