Task ID: RQ110
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-08-22
Agent/tool: Codex
Delivery target: main
Working branch / PR: local worktree / none
Main commit SHA: pending
Main verification: pending
Evidence state: pending

## What was done
- Confirmed that an owner-supplied production route/filter matrix exists for the pilot smoke surface and is sufficient to promote the RQ110 sequence out of `WAITING`.
- Claimed RQ110 locally and moved the prompt into `IN_PROGRESS`.
- Added a citable pilot screen-data availability matrix that maps dashboard, Product Decision Center, Decision Board, inventory, supplier decision/sales, and analytics actions to their current authoritative route/smoke/test evidence.
- Updated analytics queue and roadmap routing so RQ110 is the active in-progress analytics reliability prompt.
- Left the dashboard family explicitly marked as the least isolated proof surface because the reviewed evidence still does not name a separate isolated seeded-non-empty backend proof for it.
- Reviewed the latest larger React/C# work and found a real regression opportunity in `fetchWithTimeout`: timeout handling had drifted away from aborting the underlying request and still depended on browser-only `window` access.
- Fixed `fetchWithTimeout` so timeout still aborts the in-flight request, external aborts keep their own reason/message, and URL normalization no longer assumes `window.location` exists.
- Added focused Vitest coverage for timeout-abort and external-abort behavior.

## Files changed
- docs/qa/ANALYTICS_PILOT_SCREEN_DATA_AVAILABILITY_MATRIX.md
- docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
- docs/ai/ANALYTICS_RELIABILITY_PROMPT_PRIORITY_REVIEW.md
- MASTER_ROADMAP.md
- .ai/task-locks/RQ110-codex.lock.md
- .ai/runs/2026-08-22-RQ110-evidence.md
- Klijent/clientapp/src/utils/fetchWithTimeout.ts
- Klijent/clientapp/src/utils/__tests__/fetchWithTimeout.spec.ts

## Validation run
- `git diff --check` -> pass
- `node scripts/check-prompt-queues.mjs --self-test` -> pass
- `node scripts/check-prompt-queues.mjs` -> pass
- `node scripts/check-planning-architecture.mjs --self-test` -> pass
- `node scripts/check-planning-architecture.mjs` -> pass
- `npm run test:run -- src/utils/__tests__/fetchWithTimeout.spec.ts` -> pass
- `npm run typecheck` -> pass

## Validation not run
- Backend runtime tests -> not run; this task is docs/governance oriented and the current evidence relied on existing contract/integration tests plus the owner-supplied smoke matrix.
- Commit/push verification -> not run yet because the prompt remains in progress and the lock is still active.

## Documentation impact
- Added the first citable screen-data availability matrix for the pilot analytics surfaces.
- Linked the RQ110 prompt directly to that matrix and the owner-supplied smoke evidence pack.

## What was missed
- The dashboard family still lacks a separately named isolated seeded-non-empty backend proof in the reviewed evidence.
- No second concrete bug was proven in the reviewed larger commits beyond the `fetchWithTimeout` regression.

## Risks
- If the dashboard gap needs runtime proof, the next prompt should split that into a smaller dashboard-bootstrap proof instead of broadening RQ110.
- The matrix currently leans on route/meta/smoke evidence for some surfaces rather than a fully named physical table/view/materialized-view source on every row.
- The fetch timeout test covers the browser/JSDOM contract used in this repo; backend fetch semantics were not revalidated in this run.

## Next
- Decide whether to keep RQ110 in progress for a follow-up dashboard proof or to split the dashboard gap into a smaller runtime prompt.
