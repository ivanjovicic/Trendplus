Task ID: RQ218
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-09
Agent/tool: Codex
Delivery target: main
Working branch / PR: main / direct-main
Main commit SHA: pending
Main verification: pending
Evidence state: pending

## What was done

- Explicitly promoted RQ218 from WAITING to READY and claimed it as IN_PROGRESS.
- Confirmed the failure boundary: Trendplus writes already use a retriable transaction, while the separate analytics fast-path committed each projection step independently.
- Changed the analytics fast-path to use one transaction for the complete import projection, rolling back all analytics steps when any step fails before the worker requeues a transient failure.
- Added a focused source-contract regression test for the single rollback boundary and preserved existing transaction tests.

## Files changed

- `Api/Services/AccessImportService.cs`
- `Api.Tests/AccessImportRetryAtomicityTests.cs`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `MASTER_ROADMAP.md`

## Validation run

- `dotnet build Api/Api.csproj --no-restore --verbosity:minimal` -> pass, 0 errors; existing analyzer warnings remain.
- `dotnet build Api.Tests/Api.Tests.csproj --no-restore --verbosity:minimal` -> pass, 0 errors; existing analyzer warnings remain.
- `dotnet test Api.Tests/Api.Tests.csproj --no-build --filter "FullyQualifiedName~AccessImportRetryAtomicityTests|FullyQualifiedName~AccessImportExecutionStrategyTests"` -> pass, 4/4.
- `node scripts/check-agent-instructions.mjs --self-test` -> pass.
- `node scripts/check-agent-instructions.mjs` -> pass.
- `node scripts/check-prompt-queues.mjs --self-test` -> pass.
- `node scripts/check-prompt-queues.mjs` -> pass.
- `node scripts/check-planning-architecture.mjs --self-test` -> pass.
- `node scripts/check-planning-architecture.mjs` -> pass.
- `git diff --check` -> pass; Git reported only expected LF/CRLF normalization warnings.

## Validation not run

- Live Access-file plus PostgreSQL failed-import/retry proof -> not run; no provider fixture was available locally.
- Full backend test suite -> not run; focused tests cover the changed transaction contract and existing execution-strategy behavior.
- Production worker/Access smoke and remote CI -> not run; no production mutation or external source access was required.

## Documentation impact

- Promoted and closed RQ218 in `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`.
- Updated RQ routing state and completion pointer in `MASTER_ROADMAP.md`.
- Added the required scope/read/test/dependency sections to the legacy prompt as a recorded same-owner scope repair.

## What was missed

- No live provider-backed failed-import/retry execution was available locally.
- No authenticated remote migration or customer data operation was attempted.

## Risks

- Trendplus and analytics remain separate databases, so there is no distributed transaction across both stores; the analytics projection is now atomic within its own database.
- Retry correctness still depends on the existing upsert/replacement idempotency of the individual analytics operations.
- Existing analyzer warnings are baseline findings outside this prompt.

## Next

- RQ219 remains WAITING and should be explicitly promoted only when ready.
- Delivery remains pending until the implementation and synchronized evidence are pushed and `HEAD == origin/main` is freshly verified.
