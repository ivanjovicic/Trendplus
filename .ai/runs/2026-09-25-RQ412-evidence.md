Task ID: RQ412
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE_OPERATIONS_ACCURACY_ADDENDUM.md
Date: 2026-09-25
Agent/tool: Cursor Cloud Agent
Delivery target: main
Working branch / PR: cursor/supplier-shoetype-independent-oracle-b591 / direct-main
Main commit SHA: cb755cae
Main verification: passed — `git rev-parse origin/main` resolves to a commit containing `cb755cae`
Evidence state: synchronized

## What was done

- Promoted and claimed RQ412 after idle recovery (`RQ407`, `RQ411`, Supplier Sales semantic owners DONE).
- Added `SupplierShoeTypeRawFactOracle`, an implementation-independent raw SQL reference over `prodaja_stavke` / `prodaja_zaglavlje` using RQ411 `supplier_id_at_sale` and `shoe_type_id_at_sale` (not current `Artikli` master IDs).
- Added `SupplierShoeTypeIndependentOracleIntegrationTests` extending the shared RQ407 seed: all/imported/existing scope, store filter, bucket parity, share percentages, line-count invariants and article-master mutation freeze proof.
- Published deterministic manifest expectations in `docs/qa/SUPPLIER_SHOETYPE_INDEPENDENT_ORACLE_MANIFEST_2026-09-25.md`.
- Synchronized queue/roadmap truth (RQ412 DONE; stale RQ411 summary row corrected).

## Files changed

- `Api.Tests/Analytics/SupplierShoeTypeRawFactOracle.cs`
- `Api.Tests/SupplierShoeTypeIndependentOracleIntegrationTests.cs`
- `docs/qa/SUPPLIER_SHOETYPE_INDEPENDENT_ORACLE_MANIFEST_2026-09-25.md`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE_OPERATIONS_ACCURACY_ADDENDUM.md`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `MASTER_ROADMAP.md`

## Validation run

- `dotnet build Api.Tests/Api.Tests.csproj -c Release` → pass (0 errors).
- `dotnet test Api.Tests/Api.Tests.csproj -c Release --no-build --filter FullyQualifiedName~OperationsAnalyticsProofPackTests` → pass (`7/7`).

## Validation not run

- Live PostgreSQL oracle integration (`SupplierShoeTypeIndependentOracleIntegrationTests`, `4` cases) → not run; Docker/PostgreSQL host unavailable in this VM (`docker: command not found`, no `ConnectionStrings__DefaultConnection`). Tests skip unless `TRENDPLUS_RUN_INTEGRATION_TESTS=true` with a seeded database.
- Optional read-only live profile reconciliation → not run (STAB16 / provider access not configured).

## Documentation impact

- Added QA manifest for oracle semantics and expected RQ407 totals.
- Updated operations-accuracy addendum, main RQ queue header and `MASTER_ROADMAP.md` for RQ412 closure.

## What was missed

- Detail-row parity assertions against generic detail endpoints (list/total/bucket contract is covered; per-SKU detail oracle parity remains a follow-up if product wants it).

## Risks

- Residual risk until CI or an integration host runs the four new PostgreSQL tests with the RQ407 seed.
- Share-percent assertions depend on endpoint `sharePct` remaining aligned with revenue denominators.

## Next

- Promote `RQ413` (continuous Operations drift/cache guard) when the user requests the next prompt; it is sequenced ready-after RQ412.
