# RQ276 — Separate Inventory expected impact from exposure/value

## Interpreted outcome
Inventory central-action writes must not present stock exposure (`estimatedValue`) as expected business impact (`impactEstimateRsd` / `expectedImpactRsd`).

## Owner
Frontend inventory actions (`InventoryPage`, `inventoryUtils`, `ActionWorkflowPanel`).

## Contract handoff note
Backend `InventoryActionSuggestionDto` exposes `EstimatedValue` only (stock exposure). No authoritative expected-impact field exists; frontend omits impact fields and retains exposure in metadata.

## Files changed
- `Klijent/clientapp/src/components/inventory/inventoryUtils.ts` — exposure helpers + workflow queue metadata
- `Klijent/clientapp/src/pages/InventoryPage.tsx` — omit impact on queue writes; exposure in metadata
- `Klijent/clientapp/src/components/inventory/ActionWorkflowPanel.tsx` — "Vrednost zalihe" label
- Tests: `inventoryImpactSemantics.spec.ts`, updated signal/workflow panel specs
- Queue + roadmap updates

## Validation
- `npm test -- --run src/components/inventory/__tests__/inventoryImpactSemantics.spec.ts src/pages/__tests__/InventoryPage.signalActions.spec.ts src/components/inventory/ActionWorkflowPanel.spec.tsx` — 15 passed

## Delivery
- Branch: `cursor/rq276-inventory-impact-semantics-c753`
