# RQ437 evidence — repair stale Operacije tests that hide regressions

Task ID: RQ437
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-25
Agent/tool: grok
Delivery target: main (direct-main)
Working branch: none - implemented and verified in isolated detached worktree `C:\Users\Ivan\source\repos\Trendplus2-grok`, committed directly on local `main` (parent `bc248ede`)
Main commit SHA: pending - local `main` commit `test(analytics): repair stale Operacije tests (RQ437)` (parent `bc248ede`)
Main verification: pending - not yet verified on `origin/main` (this workspace does not push)
Evidence state: pending

## Why this prompt

RQ437 was the only unclaimed READY prompt when claimed (RQ434 Cursor and RQ435 Codex were IN_PROGRESS; RQ429–431 and RQ436 were WAITING; older PARTIAL/BLOCKED items are STAB16-gated). Scope is test-only and limited to five named test files.

## Root causes (test drift, no product regression found)

1. `ColorSalesStatsPage.premium.spec.tsx` looked up the ASCII label `Pokrice marze`; the product renders the Serbian label `Pokriće marže` (`ColorSalesStatsPage.tsx`).
2. `ExportSchedulerPanel.spec.tsx` expected the generic analytics fallback twice; `ExportSchedulerPanel` passes explicit per-surface safe fallbacks to `getSafeAnalyticsErrorMessage`, so technical messages map to `Rasporedi izveštaja trenutno nisu dostupni.` and `Operacija izvoza trenutno nije uspela.`. The raw-exception-hidden assertion is kept.
3. `SupplierDecisionSchemaSqlTests` expected `recommendationAllowed = exposedRecommendation.RecommendationAllowed;`; the endpoint now additionally gates on Operations integrity (`... && !blockOperationsDecisionSignals;`). The comparable-signal source is still the gate; the test now asserts the combined gate and that the ungated form is gone.
4. `SupplierDecisionSchemaSqlTests` multi-line source fragments hard-code `\n`, so they failed on a clean Windows autocrlf (CRLF) checkout; the main working tree only passed because of mixed line endings. `ReadRepoFile` (private helper in the same file) now normalizes line endings. All `DoesNotContain` guards still pass after normalization.
5. `AnalyticsCostSnapshotServiceTests.SeedComparisonFixture` seeded sale lines without sale-time attribution; supplier/shoe-type compare now groups by `SupplierIdAtSale` / `ShoeTypeIdAtSale` (RQ411 historical attribution), so both entities collapsed into one. The fixture now sets `SupplierIdAtSale`, `ShoeTypeIdAtSale` and `AttributionBasis = SaleDimensionAttribution.SaleSnapshot`.
6. `DailySalesStatsServiceTests` already passes: its stale `isključena` assertion was updated by RQ428 (`4d90ece6`). No change needed.

## Files

Changed (tests only, no product code):

- `Klijent/clientapp/src/pages/__tests__/ColorSalesStatsPage.premium.spec.tsx`
- `Klijent/clientapp/src/components/inventory/ExportSchedulerPanel.spec.tsx`
- `Api.Tests/SupplierDecisionSchemaSqlTests.cs`
- `Api.Tests/AnalyticsCostSnapshotServiceTests.cs`
- `.ai/runs/2026-09-25-RQ437-evidence.md`

Unchanged (already green): `Api.Tests/DailySalesStatsServiceTests.cs`.

## Validation (worktree; first run at `ee87b926`, targeted re-runs at `3dde5583` and `cc47d418`; commit parent `bc248ede` adds no RQ437-file change)

- `npx vitest run src/pages/__tests__/ColorSalesStatsPage.premium.spec.tsx src/components/inventory/ExportSchedulerPanel.spec.tsx` — before 12/14 (2 failed), after **14/14 passed** (2 files).
- `dotnet test Api.Tests/Api.Tests.csproj --filter "FullyQualifiedName~DailySalesStatsServiceTests|FullyQualifiedName~SupplierDecisionSchemaSqlTests|FullyQualifiedName~AnalyticsCostSnapshotServiceTests"` — before 42/47 (5 failed), after **47/47 passed**.
- Full `dotnet test Api.Tests/Api.Tests.csproj` — 1372 passed, 43 failed, 34 skipped (1449). None of the failures are in RQ437 classes; they are environment/pre-existing (Docker/Testcontainers unavailable, local Postgres login, DataSourceDiscovery/SqlServer source, SupplierNegotiationPack, AnalyticsReportsContract, cache-invalidate authorization, etc.).
- `npm run typecheck` — passed.
- `npm run check:analytics-guardrails` — self-test passed, baseline-only (50 known, 0 new, 0 removed).
- `node scripts/check-agent-instructions.mjs` (+ `--self-test`) — PASS (12 files) / PASS.
- `node scripts/check-prompt-queues.mjs` (+ `--self-test`) — PASS (558 tasks) / PASS.
- `node scripts/check-planning-architecture.mjs` (+ `--self-test`) — PASS (78 tasks) / PASS.
- `git diff --check` — passed.

## Not completed / residual risk

- Committed on local `main` only; `origin/main` containment and the DONE transition are recorded by a follow-up DONE sync.
- RQ436 (`3dde5583`) changed `ColorSalesStatsPage.tsx`; the targeted specs, guardrails/typecheck, validators and diff check were re-run on `3dde5583` and again on `cc47d418` and stay green (vitest 14/14, dotnet 47/47).
- Environment-dependent backend integration failures (Docker/Testcontainers, local Postgres credentials) remain outside RQ437.
