Task ID: RQ458
Queue: Analytics Reliability
Date: 2026-09-26
Agent/tool: Cursor Cloud Agent
Delivery target: main
Working branch / PR: `cursor/rq458-supplier-signal-identity-444b` / PR #74
Main commit SHA: pending
Main verification: pending
Evidence state: synchronized

## Interpreted outcome and owner

Preserve backend-owned Supplier Decision recommendation identity when `recommendationAllowed` is false. The frontend may block writes, but must not replace distinct row signals with `insufficient_data` or present `PRICE_NEGOTIATE` and `ASSORTMENT_REDUCE` as the same user-facing action.

## Analytics safety gate

- Source of truth: backend `recommendationCode`, `statusReason`, reason codes and trust metadata.
- Contract changed: no backend contract change; frontend presentation preserves the existing code.
- Unit/denominator: no numeric contract change.
- True zero: unchanged.
- Missing/unknown: keep unknown recommendation identity unavailable; do not use a trusted fallback.
- No baseline: unchanged.
- Freshness/fallback: preserve existing trust metadata and blocked actionability state.
- Surfaces: Supplier Decision Hub table, selected signal snapshot, report/export detail rows and action CTA.
- Tests: blocked mixed-signal fixture, existing blocked-write proof, report identity context and focused page/guardrail checks.

## Changes

- Promoted and claimed `RQ458`; `RQ459` remains WAITING due the exclusive feature-family lock.
- Preserve canonical row status from `recommendationCode` regardless of actionability permission.
- Add distinct Serbian signal labels for price negotiation and assortment reduction while retaining the blocked “Pomoćni signal” action gate.
- Expose the blocked signal identity in the explainability snapshot and report reduction rows.

## Validation

- `npm ci` -> pass using the existing `Klijent/clientapp/package-lock.json`
- `npm run test -- --run src/pages/__tests__/SupplierDecisionHubPage.spec.tsx` -> pass (`15/15`)
- `npm run check:analytics-guardrails` -> pass (encoding, baseline self-test, guardrails and TypeScript)
- `npm run build` -> pass; Vite emitted the existing large-chunk warning but completed successfully
- `git diff --check` -> pass before the implementation commits

## Files changed

- `Klijent/clientapp/src/pages/SupplierDecisionHubPage.tsx`
- `Klijent/clientapp/src/components/supplierDecisionHub/SupplierExplainabilitySnapshot.tsx`
- `Klijent/clientapp/src/services/supplierDecisionReport.ts`
- `Klijent/clientapp/src/pages/__tests__/SupplierDecisionHubPage.spec.tsx`
- `Klijent/clientapp/scripts/known-guardrail-baseline.json`
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `MASTER_ROADMAP.md`

## Not completed

- Delivery to `main` and final main-SHA synchronization.
- RQ459 KPI/chart/report population parity remains a separate WAITING prompt.

## Risks and next

- If the backend introduces a new recommendation code without updating the frontend mapping, it must remain unavailable rather than silently becoming a canonical action.
- Run the focused Supplier Decision Hub spec, analytics guardrails and frontend build, then synchronize this evidence and close RQ458 only after `origin/main` verification.
