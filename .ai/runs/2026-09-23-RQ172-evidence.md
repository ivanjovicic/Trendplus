Task ID: RQ172
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-23
Agent/tool: Codex
Delivery target: main
Working branch / PR: main / direct delivery
Main commit SHA: c9331e8cd0c8e4ac5b7eb956104777bba60854ad
Main verification: passed - fresh `git fetch origin main` confirmed `origin/main` equals `c9331e8cd0c8e4ac5b7eb956104777bba60854ad`, and the implementation SHA is an ancestor of `main`.
Evidence state: synchronized

## What was done
- Claimed RQ172 after repairing the stale current-ready pointer; the prompt was already dependency-complete and READY.
- Replaced ad-hoc embedding string serialization and `NpgsqlDbType.Unknown` with `Pgvector.Vector` parameters shared by mock and Python similarity-query paths.
- Added explicit `real` threshold and `integer` limit parameters plus finite/non-empty embedding validation.
- Enabled `UseVector()` on both production Trendplus EF registrations so raw Npgsql commands use the pgvector type mapping.
- Added a deterministic pgvector integration test using the repository's existing `pgvector/pgvector:pg16` Testcontainers fixture.
- Confirmed the existing production runtime policy rejects `EmbeddingService:UseMock=true`; its focused policy tests remain green.

## Files changed
- Api/Program.cs
- Api.Tests/EmbeddingSimilarityPgvectorIntegrationTests.cs
- Infrastructure/Services/EmbeddingService.cs
- MASTER_ROADMAP.md
- docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
- .ai/runs/2026-09-23-RQ172-evidence.md

## Validation run
- `dotnet build Api.Tests/Api.Tests.csproj --no-restore` -> pass, 0 errors; existing analyzer warnings remain.
- `dotnet test Api.Tests/Api.Tests.csproj --no-build --filter "FullyQualifiedName~EmbeddingServiceRuntimePolicyTests|FullyQualifiedName~EmbeddingSimilarityPgvectorIntegrationTests"` -> pass, 7/7. The six runtime-policy tests executed; the integration test was discovered but returned early because Docker was unavailable.
- `node scripts/check-agent-instructions.mjs --self-test; node scripts/check-agent-instructions.mjs` -> pass.
- `node scripts/check-prompt-queues.mjs --self-test; node scripts/check-prompt-queues.mjs` -> pass, 544 tasks.
- `node scripts/check-planning-architecture.mjs --self-test; node scripts/check-planning-architecture.mjs` -> pass, 78 planning tasks.
- `git diff --check` -> pass.
- `git fetch origin main` and SHA/ancestor verification -> pass.

## Validation not run
- Actual Docker-backed pgvector execution -> not run; Docker Desktop Linux engine is unavailable on this workstation.
- Remote CI result -> not inspected; CI is residual follow-up and was not a delivery gate.

## Documentation impact
- Updated the active queue date, current-ready pointer, claim/completion state and prompt-defect repair note.
- Updated MASTER_ROADMAP current RQ pointer and runtime ownership truth.

## What was missed
- The existing upload endpoint still intentionally skips embedding persistence until its separate storage/generation contract is enabled; RQ172 only hardens similarity-query binding.

## Risks
- The typed binding is locally compile-verified but the database execution proof depends on a Docker-enabled pgvector runner.
- The pre-existing mock service remains available in non-production environments; production selection is rejected by the existing runtime policy and tests.

## Next
- No next queue prompt; run the pgvector integration test on a Docker-enabled CI/validation runner.
