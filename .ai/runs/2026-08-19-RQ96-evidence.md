Task ID: RQ96
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE_INVENTORY_SIGNALS_ADDENDUM.md
Date: 2026-08-20
Agent/tool: Codex
Delivery target: main
Working branch / PR: main (fast-forwarded from cursor/queue-refill-dt09-dex20)
Main commit SHA: ce8546243a425f2fcc06841d1a58f301cd8e9c91
Main verification: passed - local main fast-forwarded to ce8546243a425f2fcc06841d1a58f301cd8e9c91
Evidence state: synchronized

Analytics safety gate:
- Source of truth: `analytics_intel.inventory_observed_daily_snapshot` for observed qty; `vw_inventory_risk_signals_v1` remains reconstructed proxy
- Contract changed? yes - new provenance contract (`observed` / `reconstructed` / `mixed` / `missing`)
- Unit/denominator: on-hand quantity at article/store/day; store_id 0 = unspecified store
- True zero case: observed `on_hand_qty = 0` with provenance observed
- Missing/unknown case: no observed row; `observed_qty` NULL; provenance missing when reconstructed also absent
- No-baseline case: reconstructed proxy is labeled, never written into the observed table
- Freshness/fallback case: missing relation returns `SnapshotAvailable=false`, not zero rows as success-with-stock
- Surfaces affected: new query/DTOs; 022 comments; intelligence README; routing docs; no inventory UI in this slice
- Tests proving table/detail/export/action parity: unit/SQL-contract tests; no export/UI surface yet

## What was done
- Added the observed daily inventory snapshot table, provenance view, MediatR query/handler and focused tests.
- Wired the new 025 SQL into `DatabaseInitializer` and the analytics smoke bootstrap.
- Updated 022 comments and the analytics routing docs to point at the observed snapshot contract.

## Validation run
- `git diff --check` - pass
- `dotnet test Api.Tests/Api.Tests.csproj --configuration Release --filter "FullyQualifiedName~ObservedInventoryDailySnapshotTests|FullyQualifiedName~AnalyticsIntelligenceSmokeTests"` - pass (12/12)

## What was missed
- No scheduled end-of-day capture worker.
- No HTTP endpoint or UI for the new query.
- Store-level observed capture from `ProductsDim` is impossible today because the source has no store granularity.

## Risks
- Without invoking the capture SQL, the observed table stays empty and the view is reconstructed-only.
- The feature still depends on source import quality for `ProductsDim.Timestamp` until a real capture worker exists.

## Next
- Promote the new observed snapshot contract into the next inventory-facing prompt only when a real capture worker or downstream consumer is ready.
