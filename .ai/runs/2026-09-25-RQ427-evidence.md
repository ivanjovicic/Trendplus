Task ID: RQ427
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-25
Agent/tool: Grok Bot (executor subagent, owner's Windows machine via Shell/Read; no cloud agent, no clone)
Delivery target: `main`
Working branch / PR: `main` (direct); no branch, no PR
Main commit SHA: `9e91817f`
Main verification: passed - `origin/main` contains `9e91817f` (verified with `git merge-base --is-ancestor` after `git fetch`); the implementation landed via the RQ432 evidence-sync commit `9e91817f` (recorded in `70bfb997`); audit prompts in `fb956bac`
Evidence state: synchronized

## What was done
- Root cause confirmed in code: `reloadNonce` was set by `retryPageLoad()` and by the same-scope data-scope handler but never read; `inventoryQuery` did not depend on it and the hook's `refetch` was used only by `refreshOperations`. For non-custom presets `retryPageLoad()` re-set identical period values, so no request was sent. The loading/error/empty branches returned before the main layout, so the control bar was unmounted.
- `retryPageLoad()` now calls `useReliableAnalyticsQuery().refetch()` after the optional preset-range refresh (batched into one render, one request). It serves the error retry, the empty-state retry, the control-bar „Osveži“ action and the scheduler `onRefresh`.
- Removed the dead `reloadNonce` state; moved the data-scope handler below the query hook, replaced the same-scope nonce bump with `refetch()` and added `refetch` to its deps.
- Extracted the control bar into `renderInventoryControls(showDataSummary)`; the main layout renders stable child slots `[page header | null]`, `[controls]`, `[page body | loading/error/empty panel]` inside the existing `ErrorBoundary`, so the controls keep their DOM identity across error -> reload -> data and in empty states. While blocking, row/page/risk chips and the inline error line are suppressed (no fake zero counts).
- Controls are hidden only before the first primary load settles (`primaryLoadSettled`), preserving the original startup behaviour.
- Filtered empty state (`filtered_out`) gets „Poništi filtere“ (clears search, store, supplier; page 1) and „Pokušaj ponovo“; unfiltered empty (`no_data`) keeps the default actions.
- Added `src/pages/__tests__/InventoryPage.retryRecovery.spec.tsx` (6 cases): error retry sends exactly one new list request and recovers the table with controls visible; the same search input stays mounted from error through reload and recovery; filtered empty state keeps controls and retry sends exactly one request; „Poništi filtere“ clears the search and reloads unfiltered data; unfiltered empty offers no reset; control-bar „Osveži“ re-runs the request exactly once without dropping the table.

## Delivery history
- `fb956bac` docs(ai): queue Operacije audit prompts RQ427-RQ437 (grok; audit prompts only).
- `9e91817f` docs(evidence): synchronize RQ432 main delivery - a concurrent workspace committed while the RQ427 files were staged, so this commit carries `InventoryPage.tsx`, the retryRecovery spec, this run log and the RQ427 queue/roadmap PARTIAL sync; `70bfb997` records that.
- Both commits were pushed to `origin/main` by the concurrent workspace; grok did not push.
- This DONE sync: `docs(ai): mark RQ427 done (inventory retry fix in 9e91817f)`.

## Files changed
- `Klijent/clientapp/src/pages/InventoryPage.tsx`
- `Klijent/clientapp/src/pages/__tests__/InventoryPage.retryRecovery.spec.tsx` (new)
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`
- `MASTER_ROADMAP.md`
- `.ai/runs/2026-09-25-RQ427-evidence.md` (new)
- Local lock `.ai/task-locks/RQ427-grok.lock.md` deleted with the DONE sync.

## Validation run
- `npm run typecheck` -> pass (before and after).
- Inventory page specs `npx vitest run src/pages/__tests__/InventoryPage` -> 17 files / 81 tests pass (two runs). Before: `InventoryPage.queueStatus.spec.tsx` 7/8, `:331` failing.
- `InventoryPage.queueStatus.spec.tsx` + `InventoryPage.retryRecovery.spec.tsx` repeat run -> 2 files / 14 tests pass.
- `npm run check:analytics-guardrails` -> pass (encoding OK; self-test OK; 50 baseline violations, 0 new; typecheck pass).
- ESLint `InventoryPage.tsx` -> 4 pre-existing problems (HEAD before the change had 5, including the unused `reloadNonce`).
- Earlier targeted baseline (74 files / 537 tests, 4 failures): `InventoryPage.queueStatus.spec.tsx:331` fixed; `DailySalesStatsPage.premium.spec.tsx` (RQ431), `ColorSalesStatsPage.premium.spec.tsx` and `ExportSchedulerPanel.spec.tsx` (RQ437) still fail as expected and were not edited.
- `npm run test:analytics` -> 890 tests: 878 pass, 12 fail; full `npm run test:run` -> 1312 tests: 1299 pass, 13 fail. None is an Inventory spec; the 9 non-baseline failures reproduce in isolation in 7 files that neither import `InventoryPage` nor were modified, and `AnalyticsDashboard.tableSystem.spec.tsx` passes alone (load-sensitive).
- Queue validators (`check-agent-instructions`, `check-prompt-queues`, `check-planning-architecture`, with and without `--self-test`) -> pass on `70bfb997` and on the DONE-sync tree before commit; `git diff --cached --check` -> pass.

## Validation not run
- Live browser/manual smoke, CI, backend tests (no backend change).

## Documentation impact
- Queue RQ427 status, summary row and completion note; RQ427 header note; `MASTER_ROADMAP.md` RQ427 note and RQ routing mention. No product docs change.

## What was missed
- Nothing in owned scope. The ASCII residual „jos“ in the Inventory empty-state reasons stays with `RQ325`.

## Risks
- Pre-existing URL/state sync race (react-router v7 transition navigation) can drop input typed in the first render cycles after mount; controls therefore stay hidden during the initial load.
- The implementation landed inside another task's commit (`9e91817f`) because of concurrent commits in a shared working tree.

## Next
- None for RQ427.
