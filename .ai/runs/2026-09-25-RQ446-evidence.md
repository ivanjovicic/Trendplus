# RQ446 run log — Shoe Type overview labels describe what is plotted and computed

Task ID: RQ446
Queue: docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md
Date: 2026-09-25
Agent/tool: Grok (executor, Ivan's local worktree)
Delivery target: local `main` only (owner instruction: local commits, never push)
Main commit SHA: pending - local `main` commit `fix(analytics): name the Shoe Type share series and average-margin basis (RQ446)`
Main verification: pending - not pushed
Evidence state: pending

## Problem

- The concentration chart `<Bar dataKey="sharePct">` (`ShoeTypeSalesStatsPage.tsx:1134`) had no `name`, so the Recharts legend and tooltip showed the raw key `sharePct`.
- The „Prosečna marža“ tooltip (`:1099`) described the total margin contribution as the basis, while the backend weights known types only (`AllEndpoints.cs:2542-2547`, `:2678`) and „Ukupan maržni doprinos“ includes the „Nepoznato“ row (`:2676`).

## Change

- Series named „Udeo u prometu %“ (same label as the margin comparison chart, `:1196`).
- Tooltip now states the known-types basis and that the „Nepoznato“ row is excluded, so it can differ from total MC / cost-covered revenue. Values unchanged; line count unchanged (guardrail baseline untouched).
- New `src/pages/__tests__/ShoeTypeSalesStatsPage.labelTruth.spec.tsx` (2 cases; the recharts mock renders the series name).

## Validation

- New spec on the old page and on the `RQ445` page: 2/2 failed; after the fix 2/2 passed.
- Focused Shoe Type set (base, premium, three `utils/__tests__/shoeType*`, `AppAnalyticsRoutes.spec.tsx`, `periodTruth`, `labelTruth`): 104/104.
- `npx tsc -b`: pass. `npm run check:analytics-guardrails`: pass.
- Queue validators (six) and `git diff --check`: pass (commit tool).
- Not run: live proof, CI, `dotnet test` (no backend change).

## Residual risk

- Whether the KPI should show the all-rows margin (or both) is an owner decision recorded in `RQ449`.
