# RQ429 evidence — Daily Sales truthfulness

Date: 2026-09-25
Queue: RQ429 — Make Daily Sales empty state, incomplete-shift counts and MA7 baseline truthful
Owner: Analytics Frontend / Daily Sales
Delivery target: `main`

## Outcome

Delivered the Daily Sales frontend/contract correction. A successful backend response with `meta.emptyReason = "no_data_in_period"` now renders the shared empty state even when the API includes calendar rows of zeroes, and suppresses zero KPIs, comparison/quality/trend panels and insights. A response with real sales evidence still renders genuine zero-sales days as `0`.

Shift evidence now distinguishes `Dani bez satnice` (missing) from `Dani sa nepotpunom satnicom` (missing plus partial), with matching values and tooltip definitions across the quality panel and shift-mix mini-stats. The MA7 baseline uses seven complete prior days and excludes the current day; insufficient history remains `null`, so those points cannot become anomaly flags. The item-count tooltip explicitly states that the total includes `Ostali`.

## Files changed

- `Klijent/clientapp/src/pages/DailySalesStatsPage.tsx`
- `Klijent/clientapp/src/pages/__tests__/DailySalesStatsPage.numericState.spec.ts`
- `Klijent/clientapp/src/pages/__tests__/DailySalesStatsPage.premium.spec.tsx`
- `Klijent/clientapp/scripts/known-guardrail-baseline.json` — moved the existing reviewed Daily Sales `setData(null)` baseline line from 745 to 749 after the scoped edit; no new guardrail violation.
- `docs/ai/ANALYTICS_RELIABILITY_PROMPT_QUEUE.md`

## Validation

- Focused RQ429 tests: **4 passed** across `DailySalesStatsPage.numericState.spec.ts` and the relevant `DailySalesStatsPage.premium.spec.tsx` cases.
- `npm run check:encoding`: passed.
- Guardrail self-test: passed.
- `node ./scripts/check-analytics-guardrails.mjs`: passed with 50 reviewed baseline violations and 0 removed.
- `npm run typecheck`: passed.
- `git diff --check`: passed.

## Not completed / residual

- .NET/backend tests were not required: RQ429 makes no backend change.
- The complete Daily Sales premium spec still contains one pre-existing concentration-contract expectation (`does not reconcile contradictory supplier totals...`) whose fixture expects a warning for a signed negative `Ostali` remainder; the current concentration owner intentionally permits that signed remainder. It is outside RQ429 and was not changed.
- Build was not run because the focused frontend proof plus typecheck and guardrails cover the changed surface; no backend contract was modified.

## Delivery proof

- Queue claim and completion metadata are recorded in the canonical queue.
- The local RQ429 lock is removed before commit.
- Final proof must record the exact implementation/closure SHA and verify `origin/main` contains it.
