# RQ273 Evidence Log

**Date**: 2026-09-15  
**Task**: Prove Inventory export/print period and scope parity  
**Status**: DONE

## Outcome

Confirmed export/print artifacts are **current-stock snapshots** (not screen signal-window views). Fixed ambient `dataScope` loss by passing explicit `dataScope` in export/preview request bodies and applying it in backend dataset building. Added UI and artifact metadata documenting the contract difference.

## Contract Decision

- **Screen table signals**: use `inventorySignalWindow` (30-day sales period) for stock cover/sell-through
- **Server export/preview**: snapshot of current inventory state with store/supplier/search/sort/`dataScope` filters; aging from movement history; does not include screen signal-window metrics

## Files Changed

| File | Change |
|------|--------|
| `Api/Dtos/InventoryExperienceDtos.cs` | Added `DataScope` to export DTO |
| `Api/Endpoints/InventoryEndpoints.cs` | Pass dataScope to dataset builder; metadata/filters for report scope |
| `Klijent/clientapp/src/services/analyticsApi.ts` | Include `dataScope` in export/preview POST bodies |
| `Klijent/clientapp/src/components/inventory/inventoryUtils.ts` | Scope labels + export contract note helper |
| `Klijent/clientapp/src/pages/InventoryPage.tsx` | Pass dataScope; show contract note; track scope changes |
| `Klijent/clientapp/src/components/inventory/ExportSchedulerPanel.tsx` | Optional `contractNote` display |
| Tests | `inventoryExportContract.spec.ts`, `inventoryExportApi.spec.ts` |

## Validation

- Frontend tests: 3/3 passed (contract helpers + API body)
- Frontend build: succeeded
- Backend build: not run (`dotnet` unavailable in environment)

## Delivery

- Branch: `cursor/rq273-inventory-report-parity-c753`
- PR: pending creation
