# Operacije second-pass classification — 2026-09-25

Task: `RQ408`  
Scope: classify `OP2-01` through `OP2-49`; no runtime product fix, migration or production-data mutation.  
Evidence rule: a static smell is `potential`, not a confirmed defect. Existing completed or active owners are `duplicate` for this decomposition and are not reopened without a new failing proof.

## Result

The catalogue contains nine still-potential findings and forty findings already covered by an existing owner or proof. No confirmed independent defect remains that warrants a new follow-up prompt. The existing `RQ414`–`RQ426` decomposition is the collision-safe follow-up set for the confirmed source-population, identity, pagination, action-lineage, denominator and projection risks; all of those prompts are already represented in the canonical Operations accuracy addendum.

| Finding | Status | Exact evidence / affected route | Source-of-truth owner and impact | Minimal reproducer or disposition |
|---|---|---|---|---|
| OP2-01 | potential | Date-only boundary construction in Supplier/Shoe Type/Color versus Daily Sales half-open requests; cited in `RQ408` catalogue. | Date-range contract; Supplier/Shoe Type/Color/Daily Sales APIs. A one-second boundary can exclude or double-count a day. | Same tenant, adjacent whole-day requests, records exactly at `00:00:00` and `23:59:59.999`; no failing runtime assertion yet. No new prompt. |
| OP2-02 | duplicate | Supplier scope-event and standalone scope paths are covered by `RQ419` and `RQ425`. | Existing scope-event owners; stale rows/trust metadata are the impact. | `RQ419` and `RQ425` completion evidence proves the owned cases; do not reopen without a residual failing case. |
| OP2-03 | duplicate | Inventory article-origin versus sale-header-origin join risk is `RQ408` catalogue OP2-03/21; list parity is `RQ414`, broader cached signals are `RQ371`/`RQ413`, historical dimensions are `RQ411`. | Inventory data-origin predicate and historical attribution owners. Mixed-source velocity is the impact. | `RQ414` owns `/inventory/list`; remaining broader drift is explicitly sequenced to `RQ371`/`RQ413`; no competing prompt. |
| OP2-04 | potential | Legacy Supplier route, canonical overview and Supplier Footwear/assortment route have different possible filter/detail contracts; no failing route pair in current proof. | Canonical route/API contract. Silent dataset changes would affect decisions. | Load the same URL/filter through each route and compare requested/effective metadata, identifiers and denominator; not reproduced. No new prompt. |
| OP2-05 | potential | Local quality/status projections coexist with backend `meta.dataQualityStatus`; existing page code and catalogue identify the seam. | Backend analytics meta is authoritative; contradictory header/row/CTA state would mislead actionability. | Feed missing, warning, critical and blocked payloads to Supplier Sales, Shoe Type and Color and compare header/row/CTA; RQ421 and completed Color owners cover known cases, no residual failure shown. |
| OP2-06 | duplicate | Eight-route proof is now `RQ407` DONE; independent Supplier/Shoe Type oracle and drift remain `RQ412`/`RQ413`. | Shared proof-pack owner; duplicate proof systems would diverge. | RQ407 live PostgreSQL proof passed 1/1 for all eight families; no new proof owner. |
| OP2-07 | duplicate | `supplierSalesStatsApi.ts` response-schema seam is explicitly owned by `RQ379`. | Supplier Sales response schema; missing validation can admit unsafe payloads. | Continue under `RQ379`; no separate prompt. |
| OP2-08 | duplicate | Supplier Sales derived memo dependency risk is `RQ420`, already DONE. | Supplier Sales derived projections; stale shares/costs are the impact. | Existing RQ420 focused proof covers changed totals; reopen only with a new failing dependency case. |
| OP2-09 | duplicate | Visible versus full Supplier Sales population is `RQ373`, already DONE. | Supplier Sales display/reference cohort; denominator drift is the impact. | Existing RQ373 parity evidence covers KPI/chart/table/detail/export; no duplicate. |
| OP2-10 | duplicate | Unknown-supplier denominator is part of the `RQ373` visible/reference population contract and related Supplier Sales denominator owners. | Supplier Sales denominator metadata; unknown revenue must not disappear silently. | Reuse RQ373 evidence and existing denominator contract; no new prompt. |
| OP2-11 | duplicate | Blocked status projection is `RQ421`, already DONE. | Backend recommendation status remains authoritative; collapsing it hides review/do-not-trust meaning. | Existing blocked-status fixtures cover row/count/detail/export; no reopen. |
| OP2-12 | duplicate | Cost-quality label semantics are owned by `RQ378` and the Supplier Sales cost contract. | Cost provenance/quality metadata; estimated or snapshot cost must not be called historical. | Reuse `RQ378`; no new lane. |
| OP2-13 | duplicate | Supplier detail identity and unknown-name snapshot behavior are owned by `RQ374`. | Supplier detail identity; collision/stale snapshot is the impact. | Use RQ374 invalid/unknown/duplicate-name fixtures; no new prompt. |
| OP2-14 | potential | Supplier Sales response-window hint compares whole-day selection with `dataWindowFrom/dataWindowTo`; static seam is cited in the catalogue. | Requested/effective period contract; false outside-window messaging can hide overlapping data. | Overlapping timezone/whole-day windows including final timestamp; no failing assertion in current evidence. No new prompt. |
| OP2-15 | duplicate | Inventory secondary calls missing period/scope are the bounded `RQ371` owner. | Inventory cached secondary-signal period/scope; omitted dimensions cause stale/mixed signals. | RQ371 owns forecast/alerts/rebalance/size-curve request parity; no duplicate. |
| OP2-16 | duplicate | Client filtering after capped alert fetch is `RQ372`, already DONE. | Server-side alert severity/count contract; false-empty and wrong counts are the impact. | RQ372 server-filter/count proof covers this case. |
| OP2-17 | duplicate | Inventory all-or-nothing secondary lifecycle is `RQ369` partial-failure coverage. | Per-panel error/empty/degraded state; one failed panel must not erase usable panels. | Use RQ369 focused failure fixtures; no competing owner. |
| OP2-18 | potential | One global Inventory loading value is copied to all secondary panels; static seam is cited in the catalogue. | Page orchestration and per-panel state; unrelated latency can mask usable data. | Delay one secondary request while another succeeds and inspect panel-specific state; no runtime failure captured. No new prompt. |
| OP2-19 | potential | `resolveSecondarySnapshotFreshness` uses latest timestamp with worst status; mixed freshness lineage is static evidence. | Snapshot freshness/provenance contract; a fresh timestamp with stale status is ambiguous. | Two sources with one stale/one fresh timestamp and worst status; no agreed contract/failing UI proof. No new prompt. |
| OP2-20 | duplicate | Missing inventory cost/no-fake-zero behavior is covered by existing no-fake-zero and Inventory action owners, including `RQ353`/`RQ416` boundaries. | Unknown cost must remain unavailable/degraded, never trusted zero. | Reuse existing missing-cost fixtures; do not create a second no-fake-zero owner. |
| OP2-21 | duplicate | Inventory list sale-origin mismatch is `RQ414`, already DONE. | Joined article/sale origin predicate; mixed-source sell-through is the impact. | RQ414 imported/existing/all fixtures are the proof. |
| OP2-22 | duplicate | Inventory deterministic paging is `RQ415`, already DONE. | Stable list ordering; equal sort keys must not move rows between pages. | RQ415 tie-breaker proof covers this finding. |
| OP2-23 | duplicate | Inventory insight identity/cost provenance is `RQ416`, already DONE. | SKU/store/supplier identity and cost provenance; wrong detail or invented cost is the impact. | RQ416 focused identity/provenance proof covers this finding. |
| OP2-24 | duplicate | Inventory size-alert context is `RQ417`, already DONE. | SKU, size and store identity; dropped context opens the wrong curve. | RQ417 size/store navigation proof covers this finding. |
| OP2-25 | duplicate | Inventory action dataset idempotency is `RQ418`, already DONE. | Action source key must include dataset dimensions; cross-dataset deduplication is the impact. | RQ418 period/scope/snapshot proof covers this finding. |
| OP2-26 | potential | `inventoryUtils.ts` computes `dueAtUtc` from `Date.now()` during render; static side effect is cited in the catalogue. | Backend workflow/action contract; repeated render could mutate due dates. | Render the same action twice with a controlled clock and compare payload/source key; no failing test or owner proof found. No new prompt. |
| OP2-27 | potential | Inventory signal window uses `reloadNonce` and current scope; possible cache churn is static evidence. | Cache/request lineage; changing cache identity on reload can reduce reproducibility. | Reload the same scope without data change and compare cache key/request window; no failing runtime proof. No new prompt. |
| OP2-28 | duplicate | Inventory page-only risk/KPI population semantics remain within `RQ371`/`RQ372` and shared RQ407 proof boundaries. | Population-level versus page-level KPI contract; labels must preserve scope. | Existing Inventory owner fixtures and RQ407 route proof cover the shared surface; no duplicate. |
| OP2-29 | duplicate | Forecast size/store risk aggregation is `RQ426`, already DONE. | Forecast row grain and SKU/store aggregation; max across unrelated rows can inflate risk. | RQ426 same-SKU/store aggregation proof covers this finding. |
| OP2-30 | duplicate | Supplier Footwear standalone scope default is `RQ425`, already DONE. | Requested/effective data scope; silent fallback to all data is the impact. | RQ425 standalone/reload/export evidence covers this finding. |
| OP2-31 | duplicate | Supplier Footwear top-eight denominator is `RQ422`, already DONE. | Full comparable cohort versus display slice; top eight must not normalize to false 100%. | RQ422 nine-category/full-cohort proof covers this finding. |
| OP2-32 | duplicate | Supplier Footwear elasticity weighting is `RQ422`, already DONE. | Backend weighted/coverage-aware elasticity; sparse article rows must not dominate. | RQ422 weighting metadata and focused tests cover this finding. |
| OP2-33 | duplicate | Supplier Footwear/vendor share versus headline total overlaps `RQ373` Supplier Sales population and `RQ422` type-insight denominator ownership. | Comparable cohort and headline total provenance; inconsistent totals mislead concentration. | Reuse those owners; no independent failing payload remains. |
| OP2-34 | duplicate | Supplier Footwear truncation/completeness is `RQ406` plus `RQ422`, both DONE. | Truncated detail cannot produce authoritative type/elasticity insight. | RQ406/422 completeness metadata and tests cover this finding. |
| OP2-35 | duplicate | Shoe Type identity is covered by existing `RQ375`-`RQ377` identity/detail contracts; Color is explicitly closed by `RQ398`. | Null/unknown identity; row/detail/snapshot collisions are the impact. | Use existing Shoe Type and RQ398 Color identity fixtures; no reopen without a new failure. |
| OP2-36 | duplicate | Concentration reconstruction overlaps `RQ376`, `RQ394` and `RQ400` denominator/decision-score contracts. | Backend-owned totals/score and explicit unknown/negative denominator. | Existing contract fixtures cover unknown, net-zero and incomplete cases. |
| OP2-37 | duplicate | Trust/status projection overlaps Shoe Type/Color owners `RQ377`, `RQ397` and `RQ400`. | Backend status/meta must agree across header, rows, detail and CTA. | Existing missing/warning/critical/blocked fixtures are the proof. |
| OP2-38 | duplicate | Snapshot/detail/export parity is owned by `RQ377` and `RQ397` plus the completed Color provenance/score owners. | Requested/effective period, scope, actionability and unavailable metrics must round-trip. | Reuse existing projection parity tests; no new prompt. |
| OP2-39 | duplicate | Daily Sales concentration is within `RQ381`/`RQ384` denominator contracts. | Full signed/unknown population versus top-N display; “Ostali” must be truthful. | Existing Daily Sales denominator owner; no new lane. |
| OP2-40 | duplicate | Daily Sales returned rows versus authoritative totals is `RQ381`/`RQ384`. | Pagination/truncation and partial-day totals; page sum must not masquerade as total. | Reuse existing Daily Sales total/population fixtures. |
| OP2-41 | duplicate | Shift/off-shift denominator semantics are `RQ382`/`RQ383`. | Accounted versus full-day share; partial/off-shift rows need explicit denominator. | Existing shift/off-shift contract; no duplicate. |
| OP2-42 | duplicate | Daily Sales mismatch precision/evidence state is within `RQ384`. | Signed/unknown quantity comparison; partial evidence must not become hard mismatch. | Existing mismatch fixtures and metadata owner cover it. |
| OP2-43 | duplicate | Daily Sales previous-period boundary is covered by `RQ385` and related period owners. | Adjacent period/DST semantics; gaps and overlaps affect comparison. | Reuse existing previous-period boundary proof. |
| OP2-44 | duplicate | Pre/Post detail-derived metrics under cap are `RQ386`, already DONE. | Capped article detail must not look like full vendor explanation. | RQ386 completeness/driver proof covers this finding. |
| OP2-45 | duplicate | Pre/Post focused concentration overlaps the existing `RQ387` focus/denominator contract. | Focused versus full population; “Ostali” must use the declared denominator. | Existing Pre/Post focus fixtures; no new prompt. |
| OP2-46 | duplicate | Pre-Nivelacija focus pagination parity is `RQ423`, already DONE. | Focus must filter server-side before pagination; page-local filtering hides candidates. | RQ423 focus population proof covers this finding. |
| OP2-47 | duplicate | Pre-Nivelacija action-share top-seven denominator is `RQ424`, already DONE. | Full leaderboard action score versus top-seven display; missing “Ostali” is the impact. | RQ424 denominator evidence covers this finding. |
| OP2-48 | duplicate | Pre-Nivelacija percentage-unit normalization is `RQ424`, already DONE. | Ratio and percentage-point units must not be inferred ambiguously. | RQ424 exact-unit fixtures cover `1`, `0.5` and `100` semantics. |
| OP2-49 | duplicate | Global versus returned-page Pre-Nivelacija population is `RQ423`/`RQ424` plus existing pagination/focus owners. | Summary, page rows, detail and export must declare their population. | RQ423/424 projection proof covers this finding. |

## Queue disposition

- Confirmed independent findings: none after the current `main` evidence and owner matrix.
- New follow-up prompts: none; creating another prompt would duplicate `RQ414`–`RQ426` or reopen an owner without a new failing proof.
- Potential findings retained for a future owner only if a focused runtime reproducer demonstrates wrong output: `OP2-01`, `OP2-04`, `OP2-05`, `OP2-14`, `OP2-18`, `OP2-19`, `OP2-26`, `OP2-27` and `OP2-33`.
- `RQ407` is consumed as completed shared proof; `RQ412`/`RQ413` remain independent future oracle/drift owners and are not folded into this prompt.
